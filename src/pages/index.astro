---
import BaseLayout from '../layouts/BaseLayout.astro';
import TrackedNetworksMarquee from '../components/TrackedNetworksMarquee.astro';
import { getCollection } from 'astro:content';

const allNetworks = await getCollection('networks');
const allNews = await getCollection('news');

const publishedNetworks = allNetworks
  .filter(n => n.data.published)
  .sort((a, b) => a.data.priority - b.data.priority);

const top6 = publishedNetworks.slice(0, 6);

const publishedNews = allNews
  .filter(n => n.data.published)
  .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());

// Curated news: pick top featured/major signals, then fill with recent
const majorNews = publishedNews.filter(n => n.data.significance === 'major' || n.data.featured);
const curatedNews = [...majorNews.slice(0, 3)];
if (curatedNews.length < 4) {
  const slugsUsed = new Set(curatedNews.map(n => n.data.slug));
  for (const n of publishedNews) {
    if (curatedNews.length >= 4) break;
    if (!slugsUsed.has(n.data.slug)) curatedNews.push(n);
  }
}

// Provider descriptor map for top 6
const descriptors: Record<string, string> = {
  'glg': 'Broad enterprise coverage and scale',
  'alphasights': 'Fast sourcing and managed service',
  'third-bridge': 'Library-rich research workflows',
  'guidepoint': 'Large advisor network and flexible access',
  'dialectica': 'Hard-to-find experts and research-heavy sourcing',
  'alphasense-tegus': 'AI-led search plus expert content access',
  'capvision': 'Asia-Pacific depth and global reach',
  'atheneum': 'Global coverage with consulting-oriented service',
};
---

<BaseLayout
  title="Best Expert Networks (2026): Compare 33 Providers — Pricing, Compliance, AI"
  description="Compare 33 expert network companies side-by-side. Independent profiles of GLG, AlphaSights, Third Bridge, Guidepoint, Dialectica, and 28 more — with pricing models, compliance ratings, AI capabilities, and expert coverage. Free, no sign-up."
  canonicalPath="/"
  jsonLd={{
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "ExpertNetworks.net",
    "alternateName": ["Expert Networks", "Expert Networks Directory"],
    "url": "https://expertnetworks.net",
    "description": `Independent expert network directory with ${publishedNetworks.length} provider profiles, comparisons, pricing, compliance, and industry news.`,
    "potentialAction": {
      "@type": "SearchAction",
      "target": "https://expertnetworks.net/networks?q={search_term_string}",
      "query-input": "required name=search_term_string"
    }
  }}
>

  <!-- ═══ 1. Hero — dark interactive particle surface ═══ -->
  <section class="relative overflow-hidden bg-[#0d0d0f] min-h-[220px] md:min-h-[260px] lg:min-h-[280px]" id="top">
    <canvas id="heroCanvas" class="absolute inset-0 w-full h-full" style="pointer-events: none;" role="presentation" aria-hidden="true"></canvas>
    <div class="absolute inset-0 pointer-events-none" style="background: radial-gradient(ellipse at 50% 40%, transparent 30%, rgba(13,13,15,0.55) 100%);"></div>
    <div class="relative z-10 pt-[76px] pb-10 md:pt-[92px] md:pb-14 lg:pt-[108px] lg:pb-16 px-4 md:px-6" id="heroContent">
      <div class="max-w-site mx-auto text-center">
        <h1 id="heroTitle" class="text-3xl md:text-4xl lg:text-[2.75rem] xl:text-5xl font-extrabold tracking-tight text-white leading-tight" aria-label="Compare expert networks the way serious buyers actually evaluate them">
          <span id="heroTitleText"></span><span id="heroTitleCursor" class="inline-block w-[3px] h-[0.85em] bg-white/70 align-middle ml-0.5 animate-[heroBlink_0.53s_steps(2)_infinite]"></span>
        </h1>
        <p id="heroSubtext" class="text-[#a1a1a6] text-sm md:text-base mt-4 leading-relaxed max-w-2xl mx-auto opacity-0 transition-opacity duration-700">
          Independent research on expert networks across pricing model, compliance posture, AI capabilities, transcript access, geography, and service model.
        </p>
        <div id="heroCtas" class="flex flex-col sm:flex-row gap-3 justify-center items-center mt-6 md:mt-7 opacity-0 transition-opacity duration-700">
          <a href="/compare" class="inline-flex items-center gap-1.5 px-6 py-3 md:py-2.5 bg-accent text-white text-sm font-semibold rounded-lg no-underline hover:bg-accent-hover transition-colors">
            Compare Providers
            <svg class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="9 18 15 12 9 6"/></svg>
          </a>
          <a href="/networks" class="inline-flex items-center gap-1.5 px-6 py-3 md:py-2.5 bg-[rgba(255,255,255,0.08)] text-[#e5e5ea] text-sm font-semibold rounded-lg no-underline hover:bg-[rgba(255,255,255,0.14)] transition-colors border border-[rgba(255,255,255,0.08)]">
            Browse All Networks
          </a>
          <a href="/what-is-an-expert-network" class="text-[#a1a1a6] text-xs font-medium no-underline hover:text-white transition-colors mt-1 sm:mt-0 sm:ml-2">
            What is an expert network? &rarr;
          </a>
        </div>
        <p id="heroMicro" class="text-[#6e6e73] text-[11px] mt-4 opacity-0 transition-opacity duration-700">No paywalls. No sign-up required. Public-source research.</p>
      </div>
    </div>
  </section>

  <style>
    @keyframes heroBlink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0; }
    }
  </style>

  <script>
  // @ts-nocheck
  // ─── Typewriter effect for hero title ───
  (function() {
    const TITLE_TEXT = 'Compare expert networks the way serious buyers actually evaluate them';
    const TYPE_DELAY = 24;
    const START_DELAY = 300;
    const CURSOR_LINGER = 500;

    const textEl = document.getElementById('heroTitleText');
    const cursorEl = document.getElementById('heroTitleCursor');
    const subtext = document.getElementById('heroSubtext');
    const ctas = document.getElementById('heroCtas');
    const micro = document.getElementById('heroMicro');
    if (!textEl || !cursorEl) return;

    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
      textEl.textContent = TITLE_TEXT;
      cursorEl.style.display = 'none';
      if (subtext) subtext.style.opacity = '1';
      if (ctas) ctas.style.opacity = '1';
      if (micro) micro.style.opacity = '1';
      return;
    }

    let i = 0;
    function typeNext() {
      if (i < TITLE_TEXT.length) {
        textEl.textContent += TITLE_TEXT[i];
        i++;
        setTimeout(typeNext, TYPE_DELAY);
      } else {
        if (subtext) subtext.style.opacity = '1';
        if (ctas) ctas.style.opacity = '1';
        if (micro) micro.style.opacity = '1';
        setTimeout(function() {
          cursorEl.style.transition = 'opacity 0.4s';
          cursorEl.style.opacity = '0';
        }, CURSOR_LINGER);
      }
    }

    setTimeout(typeNext, START_DELAY);
  })();
  </script>

  <script>
  // @ts-nocheck
  (function() {
    // ─── Particle system ───
    const PARTICLE_COUNT_DESKTOP = 110;
    const PARTICLE_COUNT_TABLET = 65;
    const PARTICLE_COUNT_MOBILE = 35;
    const MAX_CONNECTION_DIST = 140;
    const MAX_CONNECTION_DIST_SQ = MAX_CONNECTION_DIST * MAX_CONNECTION_DIST;
    const LINE_OPACITY = 0.09;
    const BASE_PARTICLE_OPACITY = 0.4;
    const IDLE_DRIFT_STRENGTH = 0.18;
    const POINTER_INFLUENCE_RADIUS = 180;
    const POINTER_INFLUENCE_RADIUS_SQ = POINTER_INFLUENCE_RADIUS * POINTER_INFLUENCE_RADIUS;
    const POINTER_REPULSION_STRENGTH = 130;
    const REVEAL_RADIUS = 200;
    const REVEAL_RADIUS_SQ = REVEAL_RADIUS * REVEAL_RADIUS;
    const REVEAL_OPACITY_BOOST = 0.5;
    const RECOVERY_SPEED = 0.018;
    const INTERPOLATION_FACTOR = 0.12;
    const CLICK_PULSE_STRENGTH = 200;
    const CLICK_PULSE_DURATION = 500;
    const DPR_CAP = 2;

    const REVEAL_PARTICLE_COUNT_DESKTOP = 140;
    const REVEAL_PARTICLE_COUNT_TABLET = 80;
    const REVEAL_PARTICLE_COUNT_MOBILE = 45;
    const REVEAL_MAX_CONNECTION_DIST = 110;
    const REVEAL_MAX_CONNECTION_DIST_SQ = REVEAL_MAX_CONNECTION_DIST * REVEAL_MAX_CONNECTION_DIST;
    const REVEAL_LINE_OPACITY = 0.22;
    const REVEAL_PARTICLE_OPACITY = 0.6;

    const canvas = document.getElementById('heroCanvas');
    const hero = document.getElementById('top');
    if (!canvas || !hero) return;
    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    const motionQuery = window.matchMedia('(prefers-reduced-motion: reduce)');
    let prefersReducedMotion = motionQuery.matches;

    function getTier() {
      const w = window.innerWidth;
      if (w >= 1024) return 'desktop';
      if (w >= 640) return 'tablet';
      return 'mobile';
    }

    function getParticleCount(tier) {
      if (tier === 'desktop') return PARTICLE_COUNT_DESKTOP;
      if (tier === 'tablet') return PARTICLE_COUNT_TABLET;
      return PARTICLE_COUNT_MOBILE;
    }

    function getRevealCount(tier) {
      if (tier === 'desktop') return REVEAL_PARTICLE_COUNT_DESKTOP;
      if (tier === 'tablet') return REVEAL_PARTICLE_COUNT_TABLET;
      return REVEAL_PARTICLE_COUNT_MOBILE;
    }

    let pointer = { x: -9999, y: -9999 };
    let smoothPointer = { x: -9999, y: -9999 };
    let isPointerInside = false;
    let clickPulse = { active: false, x: 0, y: 0, startTime: 0 };
    let particles = [];
    let revealParticles = [];
    let animationId = null;
    let isVisible = true;
    let currentTier = getTier();
    let cachedW = 0;
    let cachedH = 0;
    let lastTime = 0;

    function createParticle(w, h) {
      return {
        x: Math.random() * w,
        y: Math.random() * h,
        homeX: 0, homeY: 0,
        vx: (Math.random() - 0.5) * IDLE_DRIFT_STRENGTH,
        vy: (Math.random() - 0.5) * IDLE_DRIFT_STRENGTH,
        size: 1 + Math.random() * 1.2,
        opacity: BASE_PARTICLE_OPACITY * (0.4 + Math.random() * 0.6),
      };
    }

    function createRevealParticle(w, h) {
      return {
        x: Math.random() * w,
        y: Math.random() * h,
        size: 0.8 + Math.random() * 0.8,
        opacity: REVEAL_PARTICLE_OPACITY * (0.5 + Math.random() * 0.5),
      };
    }

    function resize() {
      const rect = hero.getBoundingClientRect();
      const dpr = Math.min(window.devicePixelRatio || 1, DPR_CAP);
      cachedW = rect.width;
      cachedH = rect.height;
      canvas.width = cachedW * dpr;
      canvas.height = cachedH * dpr;
      canvas.style.width = cachedW + 'px';
      canvas.style.height = cachedH + 'px';
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);

      const newTier = getTier();
      const needsRebuild = (newTier !== currentTier) || particles.length === 0;
      currentTier = newTier;

      if (needsRebuild) {
        const count = getParticleCount(currentTier);
        const revCount = getRevealCount(currentTier);
        particles = [];
        revealParticles = [];
        for (let i = 0; i < count; i++) {
          const p = createParticle(cachedW, cachedH);
          p.homeX = p.x;
          p.homeY = p.y;
          particles.push(p);
        }
        for (let i = 0; i < revCount; i++) {
          revealParticles.push(createRevealParticle(cachedW, cachedH));
        }
      } else {
        for (const p of particles) {
          p.homeX = Math.min(p.homeX, cachedW);
          p.homeY = Math.min(p.homeY, cachedH);
          p.x = Math.min(p.x, cachedW);
          p.y = Math.min(p.y, cachedH);
        }
      }
    }

    function onPointerMove(e) {
      const rect = hero.getBoundingClientRect();
      pointer.x = e.clientX - rect.left;
      pointer.y = e.clientY - rect.top;
      isPointerInside = true;
    }
    function onPointerLeave() { isPointerInside = false; }
    function onClick(e) {
      const rect = hero.getBoundingClientRect();
      clickPulse.active = true;
      clickPulse.x = e.clientX - rect.left;
      clickPulse.y = e.clientY - rect.top;
      clickPulse.startTime = performance.now();
    }
    function onTouchStart(e) {
      if (e.touches.length > 0) {
        const rect = hero.getBoundingClientRect();
        pointer.x = e.touches[0].clientX - rect.left;
        pointer.y = e.touches[0].clientY - rect.top;
        isPointerInside = true;
      }
    }
    function onTouchMove(e) {
      if (e.touches.length > 0) {
        const rect = hero.getBoundingClientRect();
        pointer.x = e.touches[0].clientX - rect.left;
        pointer.y = e.touches[0].clientY - rect.top;
        isPointerInside = true;
      }
    }
    function onTouchEnd() { isPointerInside = false; }

    hero.addEventListener('mousemove', onPointerMove, { passive: true });
    hero.addEventListener('mouseleave', onPointerLeave, { passive: true });
    hero.addEventListener('click', onClick);
    hero.addEventListener('touchstart', onTouchStart, { passive: true });
    hero.addEventListener('touchmove', onTouchMove, { passive: true });
    hero.addEventListener('touchend', onTouchEnd, { passive: true });
    hero.addEventListener('touchcancel', onTouchEnd, { passive: true });

    const observer = new IntersectionObserver(function(entries) {
      isVisible = entries[0].isIntersecting;
      if (isVisible && !prefersReducedMotion && !animationId) {
        lastTime = 0;
        animationId = requestAnimationFrame(draw);
      }
    }, { threshold: 0.05 });
    observer.observe(hero);

    function onVisibilityChange() {
      if (document.hidden) {
        if (animationId) { cancelAnimationFrame(animationId); animationId = null; }
      } else if (!prefersReducedMotion && !animationId) {
        lastTime = 0;
        animationId = requestAnimationFrame(draw);
      }
    }
    document.addEventListener('visibilitychange', onVisibilityChange);

    function draw(now) {
      if (!isVisible) {
        animationId = null;
        return;
      }
      animationId = requestAnimationFrame(draw);

      var dt = lastTime ? Math.min((now - lastTime) / 16.667, 3) : 1;
      lastTime = now;

      var w = cachedW;
      var h = cachedH;
      ctx.clearRect(0, 0, w, h);

      if (isPointerInside) {
        smoothPointer.x += (pointer.x - smoothPointer.x) * INTERPOLATION_FACTOR;
        smoothPointer.y += (pointer.y - smoothPointer.y) * INTERPOLATION_FACTOR;
      } else {
        smoothPointer.x += (-9999 - smoothPointer.x) * 0.02;
        smoothPointer.y += (-9999 - smoothPointer.y) * 0.02;
      }

      var pulseStrength = 0;
      var pulseRadius = 0;
      var pulseRadiusSq = 0;
      if (clickPulse.active) {
        var elapsed = now - clickPulse.startTime;
        if (elapsed > CLICK_PULSE_DURATION) {
          clickPulse.active = false;
        } else {
          var t = elapsed / CLICK_PULSE_DURATION;
          pulseStrength = CLICK_PULSE_STRENGTH * (1 - t);
          pulseRadius = POINTER_INFLUENCE_RADIUS * (1 + t * 1.5);
          pulseRadiusSq = pulseRadius * pulseRadius;
        }
      }

      var spx = smoothPointer.x;
      var spy = smoothPointer.y;
      var revealActive = spx > -1000 && spy > -1000;

      // Reveal layer
      if (revealActive) {
        ctx.strokeStyle = 'rgb(120,140,170)';
        ctx.lineWidth = 0.5;
        for (var i = 0; i < revealParticles.length; i++) {
          var a = revealParticles[i];
          var dxA = a.x - spx;
          var dyA = a.y - spy;
          var distSqA = dxA * dxA + dyA * dyA;
          if (distSqA > REVEAL_RADIUS_SQ) continue;
          var distA = Math.sqrt(distSqA);
          var alphaA = 1 - (distA / REVEAL_RADIUS);

          for (var j = i + 1; j < revealParticles.length; j++) {
            var b = revealParticles[j];
            var dxB = b.x - spx;
            var dyB = b.y - spy;
            var distSqB = dxB * dxB + dyB * dyB;
            if (distSqB > REVEAL_RADIUS_SQ) continue;

            var dxAB = a.x - b.x;
            var dyAB = a.y - b.y;
            var distSqAB = dxAB * dxAB + dyAB * dyAB;
            if (distSqAB > REVEAL_MAX_CONNECTION_DIST_SQ) continue;

            var distB = Math.sqrt(distSqB);
            var distAB = Math.sqrt(distSqAB);
            var alphaB = 1 - (distB / REVEAL_RADIUS);
            var lineFade = 1 - (distAB / REVEAL_MAX_CONNECTION_DIST);
            ctx.globalAlpha = REVEAL_LINE_OPACITY * alphaA * alphaB * lineFade;
            ctx.beginPath();
            ctx.moveTo(a.x, a.y);
            ctx.lineTo(b.x, b.y);
            ctx.stroke();
          }
        }

        ctx.fillStyle = 'rgb(140,160,190)';
        for (var i = 0; i < revealParticles.length; i++) {
          var p = revealParticles[i];
          var dx = p.x - spx;
          var dy = p.y - spy;
          var distSq = dx * dx + dy * dy;
          if (distSq > REVEAL_RADIUS_SQ) continue;
          var proximity = 1 - (Math.sqrt(distSq) / REVEAL_RADIUS);
          ctx.globalAlpha = p.opacity * proximity * REVEAL_OPACITY_BOOST;
          ctx.beginPath();
          ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      // Primary particles
      ctx.fillStyle = 'rgb(180,180,190)';
      for (var i = 0; i < particles.length; i++) {
        var p = particles[i];
        p.x += p.vx * dt;
        p.y += p.vy * dt;

        var dx = p.x - spx;
        var dy = p.y - spy;
        var distSq = dx * dx + dy * dy;
        if (distSq < POINTER_INFLUENCE_RADIUS_SQ && distSq > 0) {
          var dist = Math.sqrt(distSq);
          var proximity = 1 - dist / POINTER_INFLUENCE_RADIUS;
          var force = proximity * proximity * POINTER_REPULSION_STRENGTH;
          p.x += (dx / dist) * force * 0.04 * dt;
          p.y += (dy / dist) * force * 0.04 * dt;
        }

        if (clickPulse.active && pulseStrength > 0) {
          var cdx = p.x - clickPulse.x;
          var cdy = p.y - clickPulse.y;
          var cdistSq = cdx * cdx + cdy * cdy;
          if (cdistSq < pulseRadiusSq && cdistSq > 0) {
            var cdist = Math.sqrt(cdistSq);
            var pProx = 1 - cdist / pulseRadius;
            var pForce = pProx * pProx * pulseStrength;
            p.x += (cdx / cdist) * pForce * 0.06 * dt;
            p.y += (cdy / cdist) * pForce * 0.06 * dt;
          }
        }

        var recoveryFactor = 1 - Math.pow(1 - RECOVERY_SPEED, dt);
        p.x += (p.homeX - p.x) * recoveryFactor;
        p.y += (p.homeY - p.y) * recoveryFactor;

        if (p.x < -10) p.x = w + 10;
        if (p.x > w + 10) p.x = -10;
        if (p.y < -10) p.y = h + 10;
        if (p.y > h + 10) p.y = -10;

        ctx.globalAlpha = p.opacity;
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
        ctx.fill();
      }

      // Connections
      ctx.strokeStyle = 'rgb(180,180,190)';
      ctx.lineWidth = 0.5;
      for (var i = 0; i < particles.length; i++) {
        for (var j = i + 1; j < particles.length; j++) {
          var a = particles[i];
          var b = particles[j];
          var dxAB = a.x - b.x;
          var dyAB = a.y - b.y;
          var distSqAB = dxAB * dxAB + dyAB * dyAB;
          if (distSqAB > MAX_CONNECTION_DIST_SQ) continue;
          var distAB = Math.sqrt(distSqAB);
          var lineFade = 1 - (distAB / MAX_CONNECTION_DIST);

          var stress = 1;
          var midX = (a.x + b.x) * 0.5;
          var midY = (a.y + b.y) * 0.5;
          var midDx = midX - spx;
          var midDy = midY - spy;
          var midDistSq = midDx * midDx + midDy * midDy;
          if (midDistSq < POINTER_INFLUENCE_RADIUS_SQ) {
            stress = Math.sqrt(midDistSq) / POINTER_INFLUENCE_RADIUS;
            stress = stress * stress;
          }

          ctx.globalAlpha = LINE_OPACITY * lineFade * stress;
          ctx.beginPath();
          ctx.moveTo(a.x, a.y);
          ctx.lineTo(b.x, b.y);
          ctx.stroke();
        }
      }

      // Click pulse ring
      if (clickPulse.active) {
        var elapsed = now - clickPulse.startTime;
        var t = elapsed / CLICK_PULSE_DURATION;
        var ringRadius = 20 + t * POINTER_INFLUENCE_RADIUS * 1.2;
        ctx.strokeStyle = 'rgb(120,150,200)';
        ctx.globalAlpha = 0.15 * (1 - t);
        ctx.lineWidth = 1.5 * (1 - t);
        ctx.beginPath();
        ctx.arc(clickPulse.x, clickPulse.y, ringRadius, 0, Math.PI * 2);
        ctx.stroke();
      }

      ctx.globalAlpha = 1;
    }

    function drawStatic() {
      resize();
      ctx.clearRect(0, 0, cachedW, cachedH);
      ctx.fillStyle = 'rgb(180,180,190)';
      for (var i = 0; i < particles.length; i++) {
        var p = particles[i];
        ctx.globalAlpha = p.opacity * 0.7;
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.strokeStyle = 'rgb(180,180,190)';
      ctx.lineWidth = 0.5;
      for (var i = 0; i < particles.length; i++) {
        for (var j = i + 1; j < particles.length; j++) {
          var a = particles[i];
          var b = particles[j];
          var dxAB = a.x - b.x;
          var dyAB = a.y - b.y;
          var distSqAB = dxAB * dxAB + dyAB * dyAB;
          if (distSqAB > MAX_CONNECTION_DIST_SQ) continue;
          var distAB = Math.sqrt(distSqAB);
          var lineFade = 1 - (distAB / MAX_CONNECTION_DIST);
          ctx.globalAlpha = LINE_OPACITY * lineFade * 0.7;
          ctx.beginPath();
          ctx.moveTo(a.x, a.y);
          ctx.lineTo(b.x, b.y);
          ctx.stroke();
        }
      }
      ctx.globalAlpha = 1;
    }

    function cleanup() {
      if (animationId) { cancelAnimationFrame(animationId); animationId = null; }
      observer.disconnect();
      hero.removeEventListener('mousemove', onPointerMove);
      hero.removeEventListener('mouseleave', onPointerLeave);
      hero.removeEventListener('click', onClick);
      hero.removeEventListener('touchstart', onTouchStart);
      hero.removeEventListener('touchmove', onTouchMove);
      hero.removeEventListener('touchend', onTouchEnd);
      hero.removeEventListener('touchcancel', onTouchEnd);
      document.removeEventListener('visibilitychange', onVisibilityChange);
      window.removeEventListener('resize', debouncedResize);
      motionQuery.removeEventListener('change', onMotionChange);
    }

    function onMotionChange(e) {
      prefersReducedMotion = e.matches;
      if (prefersReducedMotion) {
        if (animationId) { cancelAnimationFrame(animationId); animationId = null; }
        drawStatic();
      } else {
        if (!animationId) { lastTime = 0; animationId = requestAnimationFrame(draw); }
      }
    }
    motionQuery.addEventListener('change', onMotionChange);

    if (prefersReducedMotion) {
      drawStatic();
    } else {
      resize();
      lastTime = 0;
      animationId = requestAnimationFrame(draw);
    }

    var resizeTimeout;
    function debouncedResize() {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(function() {
        resize();
        if (prefersReducedMotion) drawStatic();
      }, 150);
    }
    window.addEventListener('resize', debouncedResize, { passive: true });
    window.addEventListener('beforeunload', cleanup);
  })();
  </script>

  <script>
  // @ts-nocheck
  // ─── Homepage nav dark mode ───
  (function() {
    const nav = document.querySelector('nav');
    const heroSection = document.getElementById('top');
    if (!nav || !heroSection) return;

    const brandLink = nav.querySelector('a[href="/"]');
    const menuBtn = nav.querySelector('#menuBtn');
    const navLinks = nav.querySelectorAll('ul a');
    const searchBtn = nav.querySelector('#searchTrigger');
    const linkedinLink = nav.querySelector('a[aria-label="LinkedIn"]');

    let isDark = false;

    function setDarkNav() {
      if (isDark) return;
      isDark = true;
      nav.style.background = 'rgba(13,13,15,0.82)';
      nav.style.borderBottomColor = 'rgba(255,255,255,0.06)';
      if (brandLink) { brandLink.style.color = '#fff'; }
      if (menuBtn) { menuBtn.style.color = '#a1a1a6'; }
      navLinks.forEach(function(l) { l.style.color = '#a1a1a6'; });
      if (searchBtn) { searchBtn.style.background = 'rgba(255,255,255,0.08)'; searchBtn.style.color = '#a1a1a6'; }
      if (linkedinLink) { linkedinLink.style.color = '#a1a1a6'; }
    }

    function setLightNav() {
      if (!isDark) return;
      isDark = false;
      nav.style.background = '';
      nav.style.borderBottomColor = '';
      if (brandLink) { brandLink.style.color = ''; }
      if (menuBtn) { menuBtn.style.color = ''; }
      navLinks.forEach(function(l) { l.style.color = ''; });
      if (searchBtn) { searchBtn.style.background = ''; searchBtn.style.color = ''; }
      if (linkedinLink) { linkedinLink.style.color = ''; }
    }

    function check() {
      const heroBottom = heroSection.getBoundingClientRect().bottom;
      if (heroBottom > 52) {
        setDarkNav();
      } else {
        setLightNav();
      }
    }

    check();
    window.addEventListener('scroll', check, { passive: true });
    window.addEventListener('resize', check, { passive: true });
  })();
  </script>

  <!-- ═══ 2. Trust / proof strip ═══ -->
  <section class="py-4 px-4 md:px-6 border-b border-border/40 bg-bg-primary">
    <div class="max-w-site mx-auto flex flex-wrap justify-center gap-x-6 gap-y-2 text-[11px] md:text-xs text-tertiary font-medium">
      <span class="flex items-center gap-1.5">
        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green"><polyline points="20 6 9 17 4 12"/></svg>
        {publishedNetworks.length}+ providers tracked
      </span>
      <span class="flex items-center gap-1.5">
        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green"><polyline points="20 6 9 17 4 12"/></svg>
        Public-source research
      </span>
      <span class="flex items-center gap-1.5">
        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green"><polyline points="20 6 9 17 4 12"/></svg>
        Pricing, compliance, AI &amp; content compared
      </span>
      <span class="flex items-center gap-1.5">
        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green"><polyline points="20 6 9 17 4 12"/></svg>
        No affiliations
      </span>
      <span class="flex items-center gap-1.5">
        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green"><polyline points="20 6 9 17 4 12"/></svg>
        Continuously updated
      </span>
    </div>
  </section>

  <!-- ═══ 3. Networks Covered — interactive logo carousel ═══ -->
  <TrackedNetworksMarquee />

  <!-- ═══ 4. Decision pathways ═══ -->
  <section class="py-12 md:py-16 px-4 md:px-6">
    <div class="max-w-site mx-auto">
      <h2 class="text-xl md:text-2xl font-bold text-primary mb-8 text-center">Start with your use case</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        <a href="/compare" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover hover:-translate-y-0.5 transition-all no-underline border border-border/30">
          <div class="w-9 h-9 rounded-lg bg-accent/10 flex items-center justify-center mb-3">
            <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent"><path d="M16 3h5v5"/><path d="M8 3H3v5"/><path d="M21 3l-7 7"/><path d="M3 3l7 7"/><path d="M3 21l7-7"/><path d="M21 21l-7-7"/><path d="M16 21h5v-5"/><path d="M8 21H3v-5"/></svg>
          </div>
          <h3 class="font-bold text-primary text-sm mb-1 group-hover:text-accent transition-colors">I need to compare providers</h3>
          <p class="text-xs text-secondary">Side-by-side comparison across pricing, compliance, AI, and service model.</p>
        </a>
        <a href="/networks" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover hover:-translate-y-0.5 transition-all no-underline border border-border/30">
          <div class="w-9 h-9 rounded-lg bg-accent-purple/10 flex items-center justify-center mb-3">
            <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-purple"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
          </div>
          <h3 class="font-bold text-primary text-sm mb-1 group-hover:text-accent transition-colors">I need to browse the market</h3>
          <p class="text-xs text-secondary">Explore all {publishedNetworks.length} tracked expert networks with search and filters.</p>
        </a>
        <a href="/expert-network-pricing" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover hover:-translate-y-0.5 transition-all no-underline border border-border/30">
          <div class="w-9 h-9 rounded-lg bg-accent-green/10 flex items-center justify-center mb-3">
            <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/></svg>
          </div>
          <h3 class="font-bold text-primary text-sm mb-1 group-hover:text-accent transition-colors">I need to understand pricing</h3>
          <p class="text-xs text-secondary">How pricing models differ across calls, subscriptions, and content access.</p>
        </a>
        <a href="/what-is-an-expert-network" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover hover:-translate-y-0.5 transition-all no-underline border border-border/30">
          <div class="w-9 h-9 rounded-lg bg-accent-orange/10 flex items-center justify-center mb-3">
            <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-orange"><circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 015.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
          </div>
          <h3 class="font-bold text-primary text-sm mb-1 group-hover:text-accent transition-colors">I'm new to this category</h3>
          <p class="text-xs text-secondary">Learn how expert networks work, what they cost, and how to choose a provider.</p>
        </a>
        <a href="/news" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover hover:-translate-y-0.5 transition-all no-underline border border-border/30">
          <div class="w-9 h-9 rounded-lg bg-blue-50 flex items-center justify-center mb-3">
            <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-blue-500"><path d="M2 3h6a4 4 0 014 4v14a3 3 0 00-3-3H2z"/><path d="M22 3h-6a4 4 0 00-4 4v14a3 3 0 013-3h7z"/></svg>
          </div>
          <h3 class="font-bold text-primary text-sm mb-1 group-hover:text-accent transition-colors">I want the latest market news</h3>
          <p class="text-xs text-secondary">Track launches, M&A, AI products, and market developments.</p>
        </a>
        <a href="/best-expert-networks" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover hover:-translate-y-0.5 transition-all no-underline border border-border/30">
          <div class="w-9 h-9 rounded-lg bg-rose-50 flex items-center justify-center mb-3">
            <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-rose-500"><path d="M12 2L15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2z"/></svg>
          </div>
          <h3 class="font-bold text-primary text-sm mb-1 group-hover:text-accent transition-colors">I want to explore leading providers</h3>
          <p class="text-xs text-secondary">Curated overview of top expert networks by category and strength.</p>
        </a>
      </div>
    </div>
  </section>

  <!-- ═══ 5. Featured compare module ═══ -->
  <section class="py-12 md:py-16 px-4 md:px-6 bg-bg-secondary">
    <div class="max-w-site mx-auto">
      <h2 class="text-xl md:text-2xl font-bold text-primary mb-3">Compare leading expert networks</h2>
      <p class="text-sm text-secondary mb-8 max-w-2xl">Choose a preset to see how providers stack up, or build your own comparison.</p>

      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
        <a href="/compare?preset=leaders" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover transition-all no-underline border border-border/30 hover:-translate-y-0.5">
          <div class="text-[10px] font-bold uppercase tracking-wider text-accent mb-2">Global Leaders</div>
          <p class="text-xs text-secondary mb-3">GLG, AlphaSights, Third Bridge, Guidepoint, Dialectica, AlphaSense</p>
          <span class="text-xs font-semibold text-accent group-hover:underline">Open comparison &rarr;</span>
        </a>
        <a href="/compare?preset=ai" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover transition-all no-underline border border-border/30 hover:-translate-y-0.5">
          <div class="text-[10px] font-bold uppercase tracking-wider text-accent-purple mb-2">AI-Forward Platforms</div>
          <p class="text-xs text-secondary mb-3">AlphaSense, Techspert, Dialectica, NewtonX, ProSynth, Prosapient</p>
          <span class="text-xs font-semibold text-accent group-hover:underline">Open comparison &rarr;</span>
        </a>
        <a href="/compare?preset=library" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover transition-all no-underline border border-border/30 hover:-translate-y-0.5">
          <div class="text-[10px] font-bold uppercase tracking-wider text-accent-green mb-2">Library / Transcript-Heavy</div>
          <p class="text-xs text-secondary mb-3">AlphaSense, Third Bridge, GLG, Guidepoint, Dialectica, Arches</p>
          <span class="text-xs font-semibold text-accent group-hover:underline">Open comparison &rarr;</span>
        </a>
      </div>

      <a href="/compare" class="inline-flex items-center gap-2 px-5 py-2.5 bg-accent text-white rounded-full font-semibold text-sm no-underline hover:bg-accent-hover transition-colors">
        Open full comparison tool
        <svg class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="9 18 15 12 9 6"/></svg>
      </a>
    </div>
  </section>

  <!-- ═══ 6. Featured profiles ═══ -->
  <section class="py-10 md:py-12 px-4 md:px-6">
    <div class="max-w-site mx-auto">
      <div class="flex items-center justify-between mb-6">
        <h2 class="text-lg md:text-xl font-bold text-primary">Featured profiles</h2>
        <a href="/networks" class="text-accent text-sm font-medium no-underline hover:underline">View all {publishedNetworks.length} &rarr;</a>
      </div>
      <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-3">
        {top6.map(n => {
          const desc = descriptors[n.data.slug] || (n.data.description ? n.data.description.slice(0, 60) + '…' : n.data.type || '');
          return (
            <a href={`/networks/${n.data.slug}`} class="group bg-white rounded-card p-4 shadow-card border border-border/30 hover:shadow-card-hover hover:-translate-y-0.5 transition-all no-underline text-center">
              {n.data.logo ? (
                <img src={n.data.logo} alt={n.data.name} width="32" height="32" class="w-8 h-8 rounded-lg object-cover mx-auto mb-2" loading="lazy" />
              ) : (
                <div class="w-8 h-8 rounded-lg flex items-center justify-center text-white font-bold text-[10px] mx-auto mb-2" style={`background: linear-gradient(135deg, ${n.data.gradientFrom}, ${n.data.gradientTo})`}>
                  {n.data.shortName}
                </div>
              )}
              <div class="text-xs font-bold text-primary mb-1 group-hover:text-accent transition-colors">{n.data.name.split(' (')[0]}</div>
              <div class="text-[10px] text-tertiary leading-snug">{desc}</div>
            </a>
          );
        })}
      </div>
    </div>
  </section>

  <!-- ═══ 7. What matters now — curated news ═══ -->
  <section class="py-12 md:py-16 px-4 md:px-6 bg-bg-secondary">
    <div class="max-w-site mx-auto">
      <div class="flex items-center justify-between mb-8">
        <h2 class="text-xl md:text-2xl font-bold text-primary">What matters now</h2>
        <a href="/news" class="text-accent text-sm font-medium no-underline hover:underline">All signals &rarr;</a>
      </div>
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
        {curatedNews.map(article => {
          const formattedDate = new Date(article.data.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
          return (
            <a href={article.data.sourceUrl || `/news`} target={article.data.sourceUrl ? '_blank' : '_self'} rel="noopener noreferrer" class="group bg-white rounded-card p-5 shadow-card hover:shadow-card-hover transition-all no-underline border border-border/30 hover:-translate-y-0.5 flex flex-col">
              <div class="flex items-center gap-2 mb-2.5">
                <span class="text-[10px] font-bold uppercase tracking-wider px-2 py-0.5 rounded-full" style={`color: ${article.data.categoryColor || '#0071E3'}; background: ${article.data.categoryColor || '#0071E3'}15;`}>
                  {article.data.category}
                </span>
                {article.data.significance === 'major' && (
                  <span class="text-[10px] font-bold uppercase tracking-wider text-rose-600 bg-rose-50 px-2 py-0.5 rounded-full">Major</span>
                )}
              </div>
              <h3 class="text-[13px] font-semibold text-primary leading-snug mb-2 group-hover:text-accent transition-colors flex-1">{article.data.title}</h3>
              {article.data.whyItMatters && (
                <p class="text-[11px] text-secondary leading-relaxed mb-2">{article.data.whyItMatters}</p>
              )}
              <div class="flex items-center gap-2 text-[11px] text-tertiary mt-auto pt-2 border-t border-border/20">
                <span>{formattedDate}</span>
                <span>&middot;</span>
                <span>{article.data.sourceType || article.data.source}</span>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  </section>

  <!-- ═══ 8. How this site works — methodology / trust ═══ -->
  <section class="py-12 md:py-16 px-4 md:px-6 bg-bg-secondary">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-xl md:text-2xl font-bold text-primary mb-4">How this site works</h2>
      <p class="text-sm text-secondary leading-relaxed mb-6">This site is an independent public-source research resource covering expert networks, related research platforms, and industry developments. Provider data may reflect a mix of verified company statements, public reporting, and clearly labeled editorial inferences.</p>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-3 mb-6">
        <div class="bg-white rounded-lg p-3.5 text-center border border-border/30">
          <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green mx-auto mb-2"><polyline points="20 6 9 17 4 12"/></svg>
          <div class="text-[11px] font-medium text-primary">Public-source research only</div>
        </div>
        <div class="bg-white rounded-lg p-3.5 text-center border border-border/30">
          <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green mx-auto mb-2"><polyline points="20 6 9 17 4 12"/></svg>
          <div class="text-[11px] font-medium text-primary">No paywalls or gated access</div>
        </div>
        <div class="bg-white rounded-lg p-3.5 text-center border border-border/30">
          <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green mx-auto mb-2"><polyline points="20 6 9 17 4 12"/></svg>
          <div class="text-[11px] font-medium text-primary">No paid placement</div>
        </div>
        <div class="bg-white rounded-lg p-3.5 text-center border border-border/30">
          <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" class="text-accent-green mx-auto mb-2"><polyline points="20 6 9 17 4 12"/></svg>
          <div class="text-[11px] font-medium text-primary">Corrections welcomed</div>
        </div>
      </div>
      <div class="flex gap-4">
        <a href="/sources" class="text-xs font-semibold text-accent no-underline hover:underline">Sources &amp; methodology &rarr;</a>
        <a href="/verification" class="text-xs font-semibold text-secondary no-underline hover:text-accent hover:underline">Verification framework &rarr;</a>
      </div>
    </div>
  </section>

  <!-- ═══ 9. Final CTA ═══ -->
  <section class="py-14 md:py-20 px-4 md:px-6">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-xl md:text-2xl font-bold text-primary mb-3">Ready to narrow your shortlist?</h2>
      <p class="text-sm text-secondary mb-8 max-w-xl mx-auto">Compare providers side by side across pricing model, service format, compliance, AI capabilities, and content access.</p>
      <div class="flex flex-col sm:flex-row gap-3 justify-center">
        <a href="/compare" class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-accent text-white rounded-full font-semibold text-sm no-underline hover:bg-accent-hover transition-colors">
          Compare providers
          <svg class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="9 18 15 12 9 6"/></svg>
        </a>
        <a href="/networks" class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-white text-primary border border-border rounded-full font-semibold text-sm no-underline hover:bg-bg-secondary transition-colors">
          Browse all networks
        </a>
      </div>
    </div>
  </section>

</BaseLayout>
