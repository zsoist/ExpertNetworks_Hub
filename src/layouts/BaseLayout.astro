---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  ogType?: 'website' | 'article';
  canonicalPath?: string;
  jsonLd?: Record<string, any>;
}

const {
  title = 'Expert Network Directory & Industry Intelligence | ExpertNetworks.net',
  description = 'Independent expert network directory with 33+ provider profiles, side-by-side comparisons, pricing models, compliance frameworks, AI capabilities, and industry news. No sign-ups, no bias.',
  ogImage = '/og-default.svg',
  ogType = 'website',
  canonicalPath,
  jsonLd,
} = Astro.props;

const siteUrl = 'https://expertnetworks.net';
const canonicalUrl = canonicalPath ? `${siteUrl}${canonicalPath}` : new URL(Astro.url.pathname, siteUrl).href;
const ogImageUrl = ogImage.startsWith('http') ? ogImage : `${siteUrl}${ogImage}`;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <meta name="robots" content="index, follow" />
  <meta name="theme-color" content="#1D1D1F" />
  <title>{title}</title>

  <!-- Canonical URL -->
  <link rel="canonical" href={canonicalUrl} />

  <!-- Open Graph -->
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:type" content={ogType} />
  <meta property="og:url" content={canonicalUrl} />
  <meta property="og:image" content={ogImageUrl} />
  <meta property="og:image:alt" content={title} />
  <meta property="og:site_name" content="ExpertNetworks.net" />
  <meta property="og:locale" content="en_US" />

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={ogImageUrl} />

  <!-- Fonts (optimized loading) -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />

  <!-- Favicons -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="icon" type="image/png" sizes="48x48" href="/favicon-48.png" />
  <link rel="icon" type="image/png" sizes="96x96" href="/favicon-96.png" />
  <link rel="icon" type="image/png" sizes="192x192" href="/favicon-192.png" />
  <link rel="apple-touch-icon" href="/apple-touch-icon.png" />

  <!-- JSON-LD Structured Data -->
  {jsonLd && (
    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  )}

  <!-- Organization Schema (every page) -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "ExpertNetworks.net",
    "url": "https://expertnetworks.net",
    "logo": "https://expertnetworks.net/favicon.svg",
    "description": "Independent expert network directory and industry intelligence resource.",
    "sameAs": []
  })} />
</head>
<body>
  <Header />
  <main id="main-content">
    <slot />
  </main>
  <Footer />
  <script>
    // Intersection observer for fade-in animations
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });

    document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));

    // Nav background on scroll (only on pages with hero)
    const scrollHint = document.getElementById('scrollHint');
    const hero = document.getElementById('hero');
    if (scrollHint && hero) {
      window.addEventListener('scroll', () => {
        const opacity = Math.max(0, 1 - (window.scrollY / (hero.offsetHeight * 0.3)));
        scrollHint.style.opacity = String(opacity);
        const nav = document.querySelector('nav');
        if (nav) {
          (nav as HTMLElement).style.background = window.scrollY > 50 ? 'rgba(251, 251, 253, 0.92)' : 'rgba(251, 251, 253, 0.72)';
        }
      }, { passive: true });
    }

    // FIX: Reset body overflow on page load to prevent "stuck loading" after
    // navigating away while an overlay (search, menu, sidebar) was open.
    document.body.style.overflow = '';
  </script>
</body>
</html>
