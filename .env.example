# ExpertNetworks Hub — Environment Variables
# Copy this file to .env and fill in values.
# The .env file is gitignored and should never be committed.

# Admin panel password (only needed for npm run dev)
# The admin panel and API routes do not work in static production builds.
ADMIN_PASSWORD=
